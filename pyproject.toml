[project]
name = "structureddatamcp"
version = "0.1.0"
description = "Data-driven SQLite query MCP server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.5",
    "aiosqlite>=0.20.0",
    "sqlparse>=0.5.0",
]
